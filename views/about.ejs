This is a project on smart diet planner


else{
    client.connect();
    client.query("select * from users",(err,resp) =>{
        if(!err){
            console.log("1"+resp.rows);
        }
        else{
            console.log(err.stack);
        }
    })
    console.log(result);
    client.end();
}

result = await client.query('SELECT UNAME FROM USERS WHERE EMAIL=$1 AND UPASSWORD=$2',[uemail,upass]);